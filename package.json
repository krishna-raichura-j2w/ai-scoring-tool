{
  "name": "resume-matcher-pro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Score and rank resumes against job descriptions using AI",
  "scripts": {
    "server": "PORT=5050 node --watch --experimental-sqlite server/index.js",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "dev": "concurrently -k -n server,client -c blue,green \"npm run server\" \"npm run client\"",
    "start": "node --experimental-sqlite server/index.js",
    "test:api": "node server/test-api.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mammoth": "^1.9.0",
    "multer": "^1.4.5-lts.1",
    "openai": "^4.79.0",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
